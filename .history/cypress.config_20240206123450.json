const {defineConfig} = require ( "cypress" ); 
const mysql = requer ( 'cypress-mysql' ); // <- Adiciona o módulo. 

módulo . exports = defineConfig ({ 
  e2e : { 
    setupNodeEvents ( on, config ) { 
      sqlServer. configurePlugin (on); // <- Adicionar configuração do módulo.
   }, 
}, 
    {   "db": {
          "host": "snelnxm83",
          "userName": "SYSADM",
          "password":  "IX1bscs$.",
          "server":  "snelnxm83",
          "port": "1525",
          "url": "jdbc:oracle:thin:@",
        "options": {
          "database":  "BSCSIXFQA1",
          "encrypt": true,
          "rowCollectionOnRequestComplection": true
        }  
      }
    },